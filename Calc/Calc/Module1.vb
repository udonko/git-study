Module Module1

    Sub Main()
        Dim num, cal As Integer
        num = Integer.Parse(Console.ReadLine())
        cal += num
        Console.WriteLine("＋：{0}", cal)
    End Sub

End Module
